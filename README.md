# GitOntology
The Git-Onto-Logic project is an ontology-based knowledge graph used to answer queries about the sturture and activities across Git repositories. Supports semantic querying via SPARQL using RDFLib and OWLReady2, with a lightweight Flask interface for interactive exploration.
